# Valores partilhados no repo (sem segredos). Next.js carrega isto em todos os ambientes.
# Sobrescreve em .env.local (gitignored) — ver .env.local.example.
#
# Default abaixo = API Docker no mesmo Mac (dev). Em produção (VM, nginx), usa URL alcançável
# da máquina onde corre Node — ver web/deploy/server.env.example e
# ITCS/featureLLM/docs/MANUAL_INTEGRACAO.md § 1.1 (ex.: https://llm.webplace.cc).

LLM_API_URL=http://127.0.0.1:28471
