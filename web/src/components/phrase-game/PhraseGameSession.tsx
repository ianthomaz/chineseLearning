/** Phrase-game shell — Material Symbols font; session is site-wide via AuthSessionProvider. */
export function PhraseGameSession({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
